#include <stdio.h>
#include <stdlib.h>

#include "sudoku.h"

int main() {
    // Example usage
    int board[BOARD_SIZE];
    makeArray(board, BOARD_SIZE, -1);
    Guess puzzle[BOARD_SIZE];
    // makepuzzle(board, puzzle);

    return 0;
}